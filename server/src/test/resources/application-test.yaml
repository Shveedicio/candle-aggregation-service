spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/candle-aggregation-service
    username: candle-aggregation-service
    password: candle-aggregation-service
  liquibase:
    enabled: true
    change-log: db.changelog/db.changelog-master-test.yaml

